{{ 'section-faq.css' | asset_url |  stylesheet_tag }}

<div class="page-widht faq">
    <h2 class="h1">Preguntas frecuentes</h2>
</div>

{% for block in section.blocks %}
<div class="faq_row">
    <input type="checkbox" id="pregunta_{{forloop.index}} " class="faq_toggle">
    <label for="pregunta_{{forloop.index}}" class="faq_question">{{block.settings.question}}</label>
    <div class="faq_answer">{{block.settings.answer}}</div>
</div>
  {% endfor %}

{% schema %}
  {
    "name": "FAQ",
    "tag": "section",
    "settings": [
        {
          "type": "text",
          "id": "Titulo",
          "label": "titulo"
        }

    ],
    "blocks": [
      {
        "name": "pregunta",
        "type": "question",
        "settings": [
           {
             "type": "text",
             "id": "question",
             "label": "pregunta"
           },
            {
              "type": "text",
              "id": "answer",
              "label": "respuesta"
            }
        ]
      }
    ],
    "presets": [
      {
        "name": "FAQ",
        "settings": {
          
        }
      }
    ]
}
{% endschema %}